<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>New Game added</title>
</head>
<body>

    <p>Game Created!:</p>

    <h1>{{ $data['title'] }}</h1>
    <h2>{{ $data['console'] }}</h1>
    <h3>{{ $data['clasification'] }}</h1>
    <h4>{{ $data['cost'] }} | {{ $data['price'] }}</h1>

</body>
</html>

